{
    "name": "eduverse-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "npm start --prefix backend",
        "build": "npm run build --prefix backend || npm install --prefix backend"
    }
}